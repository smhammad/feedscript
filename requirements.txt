fastapi
uvicorn[standard]
jinja2
python-multipart
aiofiles
requests
instaloader>=4.15.3
# yt-dlp is the fallback download path. It requires Python 3.10+, and on 3.9
# pip silently resolves it to a 2025 release Instagram has long since broken —
# every launcher checks for 3.10+ before building a venv so that cannot happen.
yt-dlp>=2026.7.4
openai-whisper
pywebview
