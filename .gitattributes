# Keep Windows-only files using CRLF line endings so they work natively on Windows
*.bat       text eol=crlf
*.ps1       text eol=crlf

# Everything else uses LF
*           text=auto eol=lf

# Binaries — no text conversion
*.icns      binary
*.ico       binary
*.png       binary
*.jpg       binary
*.mp4       binary
